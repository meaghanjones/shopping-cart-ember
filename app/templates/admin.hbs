{{new-service saveService="saveService" updateService="updateService"}}

<div class="row service-row">
  {{#each model as |service|}}
    {{service-tile service=service  destroyService="destroyService"}}
  {{/each}}
</div>
