import { serviceCost } from 'online-shop/helpers/service-cost';
import { module, test } from 'qunit';

module('Unit | Helper | service cost');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = serviceCost([42]);
  assert.ok(result);
});
